const links = {
  live: 'https://api.biddius.com/api/',
  local: 'http://localhost:8000/api/',
  host: 'https://api.biddius.com/api/',
};

// const current = links.local;
const current = links.live;
// const current = links.host;

export { current, links };
